name: E2E

on:
  push:
    branches: [main]
    paths:
      - "cmd/**"
      - "internal/**"
      - "apps/browser/**"
      - "apps/web/**"
      - "packages/icons/**"
      - "packages/integration-sdk-ts/**"
      - "packages/sdk/**"
      - "packages/ui/**"
      - "proto/**"
      - "buf.gen.go.yaml"
      - "buf.gen.ts.yaml"
      - "buf.yaml"
      - "deploy/**"
      - "conf/app.docker.toml"
      - "docker-compose.yml"
      - "scripts/e2e/**"
      - ".github/workflows/e2e.yml"
  pull_request:
    branches: [main]
    paths:
      - "cmd/**"
      - "internal/**"
      - "apps/browser/**"
      - "apps/web/**"
      - "packages/icons/**"
      - "packages/integration-sdk-ts/**"
      - "packages/sdk/**"
      - "packages/ui/**"
      - "proto/**"
      - "buf.gen.go.yaml"
      - "buf.gen.ts.yaml"
      - "buf.yaml"
      - "deploy/**"
      - "conf/app.docker.toml"
      - "docker-compose.yml"
      - "scripts/e2e/**"
      - ".github/workflows/e2e.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: e2e-${{ github.ref }}
  cancel-in-progress: true

env:
  BROWSER_CORES: chromium
  BROWSER_TAG: local

jobs:
  compose-smoke:
    name: Compose smoke
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4

      - name: Generate config
        run: cp conf/app.docker.toml config.toml

      - name: Build images
        run: |
          docker compose -f deploy/compose/images/docker-compose.yml build server web sparse browser

      - name: Start stack
        run: |
          docker compose --profile qdrant --profile sparse --profile browser up -d

      - name: Smoke test
        run: scripts/e2e/smoke.sh --browser

      - name: Connect Bot Groups E2E
        run: scripts/e2e/connect-bot-groups.sh

      - uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Integration WebSocket E2E
        run: scripts/e2e/integration-websocket.sh

      - name: REST residual audit
        run: scripts/e2e/rest-residual-audit.sh

      - name: Logs
        if: always()
        run: |
          docker compose --profile qdrant --profile sparse --profile browser logs --no-color --tail=200

      - name: Stop stack
        if: always()
        run: |
          docker compose --profile qdrant --profile sparse --profile browser down -v --remove-orphans
